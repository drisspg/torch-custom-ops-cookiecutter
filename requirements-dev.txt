# Development dependencies for the cookiecutter template itself
# (Not the generated projects - those have their own pyproject.toml)

# Cookiecutter for generating projects
cookiecutter>=2.1.0

# Testing
pytest>=7.0.0
pytest-cookies>=0.7.0  # For testing cookiecutter templates

# Linting
ruff>=0.4.0
pre-commit>=3.0.0


